// Paste/merge into android/app/build.gradle after `flutter create .`
// generates the platform folders (inside android { defaultConfig { ... } }).

android {
    ...
    namespace "com.dave.ai.jarvis"
    defaultConfig {
        applicationId "com.dave.ai.jarvis"
        minSdkVersion 23
        // NOTE: raised from the usual 21 to 23 — flutter_local_notifications
        // and android_alarm_manager_plus need API 23+ for reliable exact
        // alarms and notification channels. This is a deliberate change
        // from a plain minSdk 21 setup, flagged here so it's not a surprise.
        targetSdkVersion 34
        compileSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
        coreLibraryDesugaringEnabled true
    }
}

dependencies {
    // Required by flutter_local_notifications on some Android versions.
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.0.4'
}
