name: dave_ai
description: DAVE AI - Offline Voice Assistant for Master DAVID
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # YOUR PACKAGES
  flutter_animate: ^4.5.0
  workmanager: ^0.5.2
  permission_handler: ^11.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^18.0.1

  # AI PACKAGES
  whisper_ggml: ^2.6.0
  llama_flutter_android: ^0.2.6
  flutter_tts: ^4.2.3
  record: ^5.2.1
  dio: ^5.4.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
